---
title: "Tesis — 03 | Modelo Nested Logit"
author: "Juan Vicente Onetto Romero"
format:
  html:
    toc: true
    code-fold: true
jupyter: python3
freeze: true
---

# Objetivo

Este notebook estima modelos **Nested Logit** para la adopción de pago QR:

- Nivel superior: **BIP** vs **QR**
- Dentro de QR: **APP RED** vs **OTRA APP**

Se estiman dos especificaciones en cada caso:

- **generic**: betas compartidos entre `QR_RED` y `QR_OTHER` (más parsimonioso)
- **specific**: betas separados por alternativa (`QR_RED` vs `QR_OTHER`)

::: {.callout-warning}
## ⚠️ Nota importante: caminata (no incluida)

En el **modelo extendido** se omiten variables de **tiempo de caminata** porque no contamos con una medición confiable para caminatas en trasbordos. Incluir caminata con alta incertidumbre puede sesgar o “contaminar” la interpretación de los betas asociados a esperas y trasbordos.

Por lo mismo, el extendido se enfoca en:
- esperas inicial/trasbordo (calculadas),
- tiempo en vehículo (calculado y originales),
- número de trasbordos (derivado de `n_etapas_recon`).
:::

---

## 1) Setup

```{python}
#| label: setup

import os
import sys
import gc
import time
from pathlib import Path

import numpy as np
import pandas as pd
import polars as pl

# Asegurar imports del repo (config/, lib/)
PROJECT_ROOT = Path().resolve()
while PROJECT_ROOT != PROJECT_ROOT.parent:
    if (PROJECT_ROOT / "config").is_dir() and (PROJECT_ROOT / "lib").is_dir():
        if str(PROJECT_ROOT) not in sys.path:
            sys.path.append(str(PROJECT_ROOT))
        break
    PROJECT_ROOT = PROJECT_ROOT.parent
else:
    raise RuntimeError("No se encontró el directorio raíz del proyecto con 'config' y 'lib'.")

from lib.datalake import read_parquet_portable, read_csv_portable
from config import (
    HORAS_PUNTA_MANANA,
    HORAS_PUNTA_TARDE,
)

# Biogeme
import biogeme.biogeme_logging as blog
import biogeme.database as db
import biogeme.biogeme as bio
import biogeme.results_processing as br
from biogeme import models, nests
from biogeme.expressions import Beta, Variable

logger = blog.get_screen_logger(level=blog.INFO)

# Directorio “canónico” del notebook (para paths estables independiente del CWD)
BIOGEME_NOTEBOOK_DIR = PROJECT_ROOT / "03_models" / "biogeme-logit"
if not BIOGEME_NOTEBOOK_DIR.is_dir():
    raise RuntimeError(f"No se encontró el directorio esperado: {BIOGEME_NOTEBOOK_DIR}")

# Dataset definitivo (por ahora una semana, W17)
PARQUET_PATH = PROJECT_ROOT / "tmp" / "viajes_con_te_calculado_2025-W17.parquet"
PARTITION_LABEL = "2025-W17"
PARTITIONS_TO_RUN = [PARTITION_LABEL]

# Codificación de elección anidada
CHOICE_BIP = 0
CHOICE_QR_RED = 1
CHOICE_QR_OTHER = 2

# Config ejecución
APPLY_SAMPLING = True
SAMPLE_FRACTION = 0.20
SEED = 42

# Output dirs (vacíos por diseño; se re-generan al correr este notebook)
BASE_OUTPUT_DIR = BIOGEME_NOTEBOOK_DIR / "model_outputs_nl_base"
EXT_OUTPUT_DIR = BIOGEME_NOTEBOOK_DIR / "model_outputs_nl_extended"
BASE_OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
EXT_OUTPUT_DIR.mkdir(parents=True, exist_ok=True)

# Tabla caracterización (para separar QR_RED vs QR_OTHER)
CARACTERIZACION_PATH = Path("/Volumes/KINGSTON/tesis-project/raw/caracterizacion/caracterización qr_202504.csv")

print("📁 Dataset:", PARQUET_PATH)
print("📁 Outputs base:", BASE_OUTPUT_DIR)
print("📁 Outputs extendido:", EXT_OUTPUT_DIR)
print("📁 Caracterización:", CARACTERIZACION_PATH)

df_caracterizacion = None
if CARACTERIZACION_PATH.exists():
    # El CSV de caracterización viene separado por ';' (no por coma).
    # Replicamos la lógica usada en el builder de buffers para evitar errores silenciosos.
    df_raw = read_csv_portable(
        str(CARACTERIZACION_PATH),
        separator=";",
        try_parse_dates=False,
    )
    if ("id_interno" not in df_raw.columns) or ("tipo_app" not in df_raw.columns):
        raise ValueError(
            f"Caracterización inválida: faltan columnas en {str(CARACTERIZACION_PATH)!r} "
            f"(se requieren id_interno, tipo_app)."
        )

    # Guardrail: 1 fila por tarjeta (evita duplicación de viajes por join).
    # Si un id aparece múltiples veces, nos basta con saber si alguna vez fue 'APP RED'.
    df_caracterizacion = (
        df_raw.with_columns(
            [
                pl.col("id_interno").cast(pl.Utf8).alias("id_tarjeta_str"),
                pl.col("tipo_app").cast(pl.Utf8).alias("tipo_app"),
            ]
        )
        .select(["id_tarjeta_str", "tipo_app"])
        .group_by("id_tarjeta_str")
        .agg(
            pl.when((pl.col("tipo_app") == "APP RED").any())
            .then(pl.lit("APP RED"))
            .otherwise(pl.lit("OTRA APP"))
            .alias("tipo_app")
        )
    )
    print(f"✅ Caracterización cargada: {df_caracterizacion.height:,} usuarios únicos")
else:
    print("⚠️ No se encontró caracterización. Todos los QR serán clasificados como QR_OTHER.")
```

---

## 2) Helpers (features + muestreo)

```{python}
#| label: helpers

def get_stratified_sample_nested(df: pl.DataFrame, fraction: float, seed: int) -> pl.DataFrame:
    """Muestreo estratificado por la columna 'choice_nested'."""
    total_rows = len(df)
    if total_rows == 0:
        return df.clear()
    target_rows = int(total_rows * fraction)
    if target_rows == 0:
        return df.clear()

    counts = df.group_by("choice_nested").agg(pl.len().alias("count")).sort("choice_nested")
    proportions = counts.with_columns((pl.col("count") / total_rows).alias("proportion"))

    samples = []
    total_sampled = 0
    num_groups = len(proportions)

    for i, row in enumerate(proportions.iter_rows(named=True)):
        choice_code = row["choice_nested"]
        count = row["count"]
        proportion = row["proportion"]

        target_n = int(round(target_rows * proportion))
        if i == num_groups - 1:
            target_n = target_rows - total_sampled
        target_n = max(0, min(target_n, count))

        if target_n > 0:
            df_sample_group = df.filter(pl.col("choice_nested") == choice_code).sample(
                n=target_n, shuffle=True, seed=seed + i
            )
            samples.append(df_sample_group)
            total_sampled += target_n

    if not samples:
        return df.clear()

    return pl.concat(samples).sample(fraction=1.0, shuffle=True, seed=seed + num_groups)


def _join_caracterizacion(lf: pl.LazyFrame, df_caract: pl.DataFrame | None) -> pl.LazyFrame:
    lf = lf.with_columns(pl.col("id_tarjeta").cast(pl.Utf8).alias("id_tarjeta_str"))
    if df_caract is None:
        return lf.with_columns(pl.lit(None).cast(pl.Utf8).alias("tipo_app"))
    return lf.join(df_caract.lazy(), on="id_tarjeta_str", how="left")


def _build_choice_nested(lf: pl.LazyFrame) -> pl.LazyFrame:
    return lf.with_columns(
        pl.when(pl.col("is_qr") == False).then(pl.lit(CHOICE_BIP))
        .when((pl.col("is_qr") == True) & (pl.col("tipo_app") == "APP RED")).then(pl.lit(CHOICE_QR_RED))
        .when(pl.col("is_qr") == True).then(pl.lit(CHOICE_QR_OTHER))
        .otherwise(pl.lit(CHOICE_BIP))
        .cast(pl.Int8)
        .alias("choice_nested")
    )


def _build_time_dummies(lf: pl.LazyFrame) -> pl.LazyFrame:
    return (
        lf.with_columns(
            pl.col("tiempo_inicio_viaje")
            .dt.replace_time_zone("America/Santiago", ambiguous="earliest")
            .alias("ts_local")
        )
        .with_columns(
            pl.col("ts_local").dt.hour().alias("hora"),
            pl.col("ts_local").dt.weekday().alias("dia_semana"),
            (pl.col("tipodia").cast(pl.Utf8).cast(pl.Int8, strict=False) == 0).alias("is_laboral"),
        )
        .with_columns(
            pl.col("hora").is_in(HORAS_PUNTA_MANANA).alias("punta_manana"),
            pl.col("hora").is_in(HORAS_PUNTA_TARDE).alias("punta_tarde"),
        )
        .with_columns(
            (pl.col("punta_manana") & pl.col("is_laboral")).cast(pl.Int8).alias("DUMMY_LAB_PM"),
            (pl.col("punta_tarde") & pl.col("is_laboral")).cast(pl.Int8).alias("DUMMY_LAB_PT"),
            (~pl.col("is_laboral")).cast(pl.Int8).alias("DUMMY_NO_LAB"),
        )
    )


def prepare_nested_base_features(lazy_frame: pl.LazyFrame, df_caract: pl.DataFrame | None = None) -> pl.LazyFrame:
    cols = ["is_qr", "id_tarjeta", "tiempo_inicio_viaje", "tipodia"]
    schema = lazy_frame.collect_schema()
    missing = [c for c in cols if c not in schema]
    if missing:
        raise ValueError(f"Columnas faltantes (base): {missing}")

    lf = lazy_frame.select(cols)
    lf = _join_caracterizacion(lf, df_caract)
    lf = _build_choice_nested(lf)
    lf = _build_time_dummies(lf)
    return lf.select(["choice_nested", "DUMMY_LAB_PM", "DUMMY_LAB_PT", "DUMMY_NO_LAB"]).fill_null(0)


def prepare_features_extended(lazy_frame: pl.LazyFrame, df_caract: pl.DataFrame | None = None) -> pl.LazyFrame:
    """
    Modelo extendido (sin caminata):
    - tiempos de espera inicial: te0_calculado
    - tiempos de espera trasbordo: sum(te1..te5_calculado)
    - tiempo en vehículo: sum(tv1..tv6_calculado)
    - trasbordos: n_etapas_recon - 1

    Nota: trabajamos en MINUTOS por estabilidad numérica.
    """
    cols = (
        ["is_qr", "id_tarjeta", "tiempo_inicio_viaje", "tipodia", "n_etapas_recon"]
        + [f"te{i}_calculado" for i in range(6)]
        + [f"tv{i}_calculado" for i in range(1, 7)]
    )
    schema = lazy_frame.collect_schema()
    missing = [c for c in cols if c not in schema]
    if missing:
        raise ValueError(f"Columnas faltantes (extendido): {missing}")

    lf = lazy_frame.select(cols)
    lf = _join_caracterizacion(lf, df_caract)
    lf = _build_choice_nested(lf)
    lf = _build_time_dummies(lf)

    cols_te_transfer = [f"te{i}_calculado" for i in range(1, 6)]
    cols_tv = [f"tv{i}_calculado" for i in range(1, 7)]
    lf = lf.with_columns(
        pl.sum_horizontal([pl.col(c).fill_null(0) for c in cols_te_transfer]).alias("t_espera_trasbordo_calc_seg"),
        pl.sum_horizontal([pl.col(c).fill_null(0) for c in cols_tv]).alias("t_vehiculo_total_calc_seg"),
        pl.col("n_etapas_recon").fill_null(1).cast(pl.Int8).alias("n_etapas_recon"),
    ).with_columns(
        (pl.col("t_vehiculo_total_calc_seg") / 60.0).alias("VAR_T_VEHICULO_MIN"),
        (pl.col("n_etapas_recon") - 1).alias("VAR_N_TRASBORDOS"),
        (pl.col("t_espera_trasbordo_calc_seg") / 60.0).alias("VAR_T_ESPERA_TRASBORDO_MIN"),
        (pl.col("te0_calculado").fill_null(0) / 60.0).alias("VAR_T_ESPERA_INICIAL_MIN"),
    )

    return lf.select(
        [
            "choice_nested",
            "DUMMY_LAB_PM", "DUMMY_LAB_PT", "DUMMY_NO_LAB",
            "VAR_T_VEHICULO_MIN", "VAR_N_TRASBORDOS",
            "VAR_T_ESPERA_TRASBORDO_MIN", "VAR_T_ESPERA_INICIAL_MIN",
        ]
    ).fill_null(0)

print("✅ Helpers listos.")
```

---

## 2.1) Setup de corridas (particiones, muestreos y orden)

```{python}
#| label: setup-run-config

# Particiones a estimar (fácil de extender a múltiples semanas)
RUN_PARTITIONS = ["2025-W17"]

# Dataset por partición (por ahora es el mismo parquet; si agregas más semanas,
# reemplaza por un dict con paths distintos).
PARQUET_BY_PARTITION = {p: PARQUET_PATH for p in RUN_PARTITIONS}

# Configs de muestreo (usar None => full sin muestreo)
SAMPLE_CONFIGS = {
    "sample20pct": 0.20,
    "sample30pct": 0.30,
    "sample40pct": 0.40,
    # "full": None,
}

# Sample default para drivers “rápidos” (evita correr 50% sin querer).
DEFAULT_SAMPLE_LABEL = "sample20pct"

# Orden deseado de ejecución (extendido: primero specific, luego generic)
EXTENDED_RUN_ORDER = ["specific", "generic"]
BASE_RUN_ORDER = ["generic", "specific"]

print("✅ Run config")
print("- Partitions:", RUN_PARTITIONS)
print("- Sample configs:", SAMPLE_CONFIGS)
print("- Default sample:", DEFAULT_SAMPLE_LABEL)
print("- Base order:", BASE_RUN_ORDER)
print("- Extended order:", EXTENDED_RUN_ORDER)
```

---

## 2.2) Estimadores (helpers)

```{python}
#| label: estimation-helpers

def _disable_biogeme_reports(b: bio.BIOGEME) -> None:
    # Evita errores al generar HTML/YAML si Hessian/robust VC falla en corridas no-convergentes.
    try:
        b.biogeme_parameters.set_value("generate_html", False)
        b.biogeme_parameters.set_value("generate_yaml", False)
    except Exception:
        pass


def _estimate_and_write(
    *,
    b: bio.BIOGEME,
    out_dir: Path,
    model_name: str,
) -> None:
    _disable_biogeme_reports(b)

    cwd0 = os.getcwd()
    out_dir.mkdir(parents=True, exist_ok=True)
    os.chdir(out_dir)
    try:
        print(f"   🧮 Estimando: {model_name}")
        t0 = time.perf_counter()
        results = b.estimate()
        elapsed_s = time.perf_counter() - t0
    finally:
        os.chdir(cwd0)

    stats = results.get_general_statistics()
    # Biogeme llena "Bootstrapping time" solo si se ejecuta bootstrap; si no, queda None/NaN.
    # Guardamos un tiempo de pared reproducible.
    stats["Wall-clock seconds"] = float(elapsed_s)

    # Importante: Biogeme reporta (por defecto) Rho vs "init model".
    # Si el init proviene de un `.iter` o está muy cerca del óptimo, LL_init ≈ LL_final y Rho(init) ≈ 0.
    # Para comparación consistente entre corridas/frameworks, agregamos Rho vs un modelo nulo
    # con probabilidades equiprobables (3 alternativas).
    try:
        nobs = float(stats.get("Sample size", np.nan))
        ll_final = float(stats.get("Final log likelihood", np.nan))
        k_params = float(stats.get("Number of estimated parameters", np.nan))
        if np.isfinite(nobs) and np.isfinite(ll_final) and np.isfinite(k_params) and nobs > 0:
            ll_null = -nobs * float(np.log(3.0))
            stats["Null log likelihood (equal shares)"] = ll_null
            stats["Rho-square (vs null equal shares)"] = 1.0 - (ll_final / ll_null)
            stats["Rho-square-bar (vs null equal shares)"] = 1.0 - ((ll_final - k_params) / ll_null)
    except Exception:
        pass

    try:
        params_df = br.get_pandas_estimated_parameters(estimation_results=results)
    except Exception:
        params_df = br.get_pandas_estimated_parameters(
            estimation_results=results,
            variance_covariance_type=br.EstimateVarianceCovariance.BHHH,
        )

    params_df.to_csv(out_dir / f"params_{model_name}.csv")
    pd.DataFrame([stats]).to_csv(out_dir / f"stats_{model_name}.csv", index=False)

    # Liberar objetos potencialmente pesados (Hessian, matrices, etc.)
    try:
        del results, params_df, stats
    except Exception:
        pass


def estimate_nested_logit_base(
    *,
    partition: str,
    spec: str,  # "generic" | "specific"
    sample_label: str,
    sample_fraction: float | None,
    seed: int,
) -> None:
    parquet_path = PARQUET_BY_PARTITION[partition]
    year, week = partition.split("-W")

    out_dir = BASE_OUTPUT_DIR / f"W{week}-nested-base-{spec}-{sample_label}"
    model_name = f"nested_logit_base_{spec}_{partition}"

    pandas_df = None
    try:
        lf = read_parquet_portable(str(parquet_path))
        df = prepare_nested_base_features(lf, df_caract=df_caracterizacion).collect()
        if sample_fraction is not None:
            df = get_stratified_sample_nested(df, sample_fraction, seed)
        pandas_df = df.to_pandas()
    finally:
        try:
            del lf, df
        except Exception:
            pass
        gc.collect()

    CHOICE_NESTED = Variable("choice_nested")

    # Variables temporales
    PM_LAB = Variable("DUMMY_LAB_PM")
    PT_LAB = Variable("DUMMY_LAB_PT")
    NO_LAB = Variable("DUMMY_NO_LAB")

    # Nido QR
    MU_QR = Beta(f"MU_QR_{partition}", 1, 1, None, 0)
    nest_bip = nests.OneNestForNestedLogit(1.0, [CHOICE_BIP], name="BIP")
    nest_qr = nests.OneNestForNestedLogit(MU_QR, [CHOICE_QR_RED, CHOICE_QR_OTHER], name="QR")
    nests_structure = nests.NestsForNestedLogit(
        choice_set=[CHOICE_BIP, CHOICE_QR_RED, CHOICE_QR_OTHER],
        tuple_of_nests=(nest_bip, nest_qr),
    )

    V_BIP = 0

    if spec == "generic":
        ASC_QR_RED = Beta(f"ASC_QR_RED_{partition}", 0, None, None, 0)
        B_LAB_PM = Beta(f"B_LAB_PM_{partition}", 0, None, None, 0)
        B_LAB_PT = Beta(f"B_LAB_PT_{partition}", 0, None, None, 0)
        B_NO_LAB = Beta(f"B_NO_LAB_{partition}", 0, None, None, 0)

        V_COMMON_QR = B_LAB_PM * PM_LAB + B_LAB_PT * PT_LAB + B_NO_LAB * NO_LAB
        V_QR_RED = ASC_QR_RED + V_COMMON_QR
        V_QR_OTHER = 0 + V_COMMON_QR
    elif spec == "specific":
        ASC_QR_RED = Beta(f"ASC_QR_RED_{partition}", 0, None, None, 0)
        ASC_QR_OTHER = Beta(f"ASC_QR_OTHER_{partition}", 0, None, None, 0)

        B_QR_RED_LAB_PM = Beta(f"B_QR_RED_LAB_PM_{partition}", 0, None, None, 0)
        B_QR_OTHER_LAB_PM = Beta(f"B_QR_OTHER_LAB_PM_{partition}", 0, None, None, 0)
        B_QR_RED_LAB_PT = Beta(f"B_QR_RED_LAB_PT_{partition}", 0, None, None, 0)
        B_QR_OTHER_LAB_PT = Beta(f"B_QR_OTHER_LAB_PT_{partition}", 0, None, None, 0)
        B_QR_RED_NO_LAB = Beta(f"B_QR_RED_NO_LAB_{partition}", 0, None, None, 0)
        B_QR_OTHER_NO_LAB = Beta(f"B_QR_OTHER_NO_LAB_{partition}", 0, None, None, 0)

        V_QR_RED = (
            ASC_QR_RED
            + B_QR_RED_LAB_PM * PM_LAB
            + B_QR_RED_LAB_PT * PT_LAB
            + B_QR_RED_NO_LAB * NO_LAB
        )
        V_QR_OTHER = (
            ASC_QR_OTHER
            + B_QR_OTHER_LAB_PM * PM_LAB
            + B_QR_OTHER_LAB_PT * PT_LAB
            + B_QR_OTHER_NO_LAB * NO_LAB
        )
    else:
        raise ValueError(f"spec inválida: {spec}")

    V = {CHOICE_BIP: V_BIP, CHOICE_QR_RED: V_QR_RED, CHOICE_QR_OTHER: V_QR_OTHER}
    av = {CHOICE_BIP: 1, CHOICE_QR_RED: 1, CHOICE_QR_OTHER: 1}

    logprob = models.lognested(V, av, nests_structure, CHOICE_NESTED)
    biodb = db.Database(f"db_nl_base_{spec}_{partition}", pandas_df)
    b = bio.BIOGEME(biodb, logprob)
    b.model_name = model_name

    _estimate_and_write(b=b, out_dir=out_dir, model_name=model_name)
    print(f"✅ OK base {spec} | {partition} | {sample_label}")

    # Limpieza explícita para evitar acumulación entre corridas
    try:
        del pandas_df, biodb, b, logprob, V, av, nests_structure
    except Exception:
        pass
    gc.collect()
    time.sleep(1)


def estimate_nested_logit_extended(
    *,
    partition: str,
    spec: str,  # "generic" | "specific"
    sample_label: str,
    sample_fraction: float | None,
    seed: int,
) -> None:
    parquet_path = PARQUET_BY_PARTITION[partition]
    year, week = partition.split("-W")

    out_dir = EXT_OUTPUT_DIR / f"W{week}-nested-ext-{spec}-{sample_label}"
    model_name = f"nested_logit_ext_{spec}_{partition}"

    pandas_df = None
    try:
        lf = read_parquet_portable(str(parquet_path))
        df = prepare_features_extended(lf, df_caract=df_caracterizacion).collect()
        if sample_fraction is not None:
            df = get_stratified_sample_nested(df, sample_fraction, seed)
        pandas_df = df.to_pandas()
    finally:
        try:
            del lf, df
        except Exception:
            pass
        gc.collect()

    CHOICE_NESTED = Variable("choice_nested")

    # Variables temporales
    PM_LAB = Variable("DUMMY_LAB_PM")
    PT_LAB = Variable("DUMMY_LAB_PT")
    NO_LAB = Variable("DUMMY_NO_LAB")

    # Atributos viaje (en minutos)
    T_VEHICULO = Variable("VAR_T_VEHICULO_MIN")
    N_TRASBORDOS = Variable("VAR_N_TRASBORDOS")
    T_ESPERA_TRASB = Variable("VAR_T_ESPERA_TRASBORDO_MIN")
    T_ESPERA_INI = Variable("VAR_T_ESPERA_INICIAL_MIN")

    MU_QR = Beta(f"MU_QR_{partition}", 1, 1, None, 0)
    nest_bip = nests.OneNestForNestedLogit(1.0, [CHOICE_BIP], name="BIP")
    nest_qr = nests.OneNestForNestedLogit(MU_QR, [CHOICE_QR_RED, CHOICE_QR_OTHER], name="QR")
    nests_structure = nests.NestsForNestedLogit(
        choice_set=[CHOICE_BIP, CHOICE_QR_RED, CHOICE_QR_OTHER],
        tuple_of_nests=(nest_bip, nest_qr),
    )

    V_BIP = 0

    if spec == "generic":
        ASC_QR_RED = Beta(f"ASC_QR_RED_{partition}", 0, None, None, 0)

        B_LAB_PM = Beta(f"B_LAB_PM_{partition}", 0, None, None, 0)
        B_LAB_PT = Beta(f"B_LAB_PT_{partition}", 0, None, None, 0)
        B_NO_LAB = Beta(f"B_NO_LAB_{partition}", 0, None, None, 0)
        B_T_VEHICULO = Beta(f"B_T_VEHICULO_{partition}", 0, None, None, 0)
        B_N_TRASBORDOS = Beta(f"B_N_TRASBORDOS_{partition}", 0, None, None, 0)
        B_T_ESPERA_TRASB = Beta(f"B_T_ESPERA_TRASB_{partition}", 0, None, None, 0)
        B_T_ESPERA_INI = Beta(f"B_T_ESPERA_INI_{partition}", 0, None, None, 0)

        V_COMMON_QR = (
            B_LAB_PM * PM_LAB + B_LAB_PT * PT_LAB + B_NO_LAB * NO_LAB
            + B_T_VEHICULO * T_VEHICULO + B_N_TRASBORDOS * N_TRASBORDOS
            + B_T_ESPERA_TRASB * T_ESPERA_TRASB + B_T_ESPERA_INI * T_ESPERA_INI
        )
        V_QR_RED = ASC_QR_RED + V_COMMON_QR
        V_QR_OTHER = 0 + V_COMMON_QR
    elif spec == "specific":
        ASC_QR_RED = Beta(f"ASC_QR_RED_{partition}", 0, None, None, 0)
        ASC_QR_OTHER = Beta(f"ASC_QR_OTHER_{partition}", 0, None, None, 0)

        B_QR_RED_LAB_PM = Beta(f"B_QR_RED_LAB_PM_{partition}", 0, None, None, 0)
        B_QR_OTHER_LAB_PM = Beta(f"B_QR_OTHER_LAB_PM_{partition}", 0, None, None, 0)
        B_QR_RED_LAB_PT = Beta(f"B_QR_RED_LAB_PT_{partition}", 0, None, None, 0)
        B_QR_OTHER_LAB_PT = Beta(f"B_QR_OTHER_LAB_PT_{partition}", 0, None, None, 0)
        B_QR_RED_NO_LAB = Beta(f"B_QR_RED_NO_LAB_{partition}", 0, None, None, 0)
        B_QR_OTHER_NO_LAB = Beta(f"B_QR_OTHER_NO_LAB_{partition}", 0, None, None, 0)

        B_QR_RED_T_VEHICULO = Beta(f"B_QR_RED_T_VEHICULO_{partition}", 0, None, None, 0)
        B_QR_OTHER_T_VEHICULO = Beta(f"B_QR_OTHER_T_VEHICULO_{partition}", 0, None, None, 0)
        B_QR_RED_N_TRASBORDOS = Beta(f"B_QR_RED_N_TRASBORDOS_{partition}", 0, None, None, 0)
        B_QR_OTHER_N_TRASBORDOS = Beta(f"B_QR_OTHER_N_TRASBORDOS_{partition}", 0, None, None, 0)
        B_QR_RED_T_ESPERA_TRASB = Beta(f"B_QR_RED_T_ESPERA_TRASB_{partition}", 0, None, None, 0)
        B_QR_OTHER_T_ESPERA_TRASB = Beta(f"B_QR_OTHER_T_ESPERA_TRASB_{partition}", 0, None, None, 0)
        B_QR_RED_T_ESPERA_INI = Beta(f"B_QR_RED_T_ESPERA_INI_{partition}", 0, None, None, 0)
        B_QR_OTHER_T_ESPERA_INI = Beta(f"B_QR_OTHER_T_ESPERA_INI_{partition}", 0, None, None, 0)

        V_QR_RED = (
            ASC_QR_RED
            + B_QR_RED_LAB_PM * PM_LAB + B_QR_RED_LAB_PT * PT_LAB
            + B_QR_RED_NO_LAB * NO_LAB
            + B_QR_RED_T_VEHICULO * T_VEHICULO + B_QR_RED_N_TRASBORDOS * N_TRASBORDOS
            + B_QR_RED_T_ESPERA_TRASB * T_ESPERA_TRASB + B_QR_RED_T_ESPERA_INI * T_ESPERA_INI
        )
        V_QR_OTHER = (
            ASC_QR_OTHER
            + B_QR_OTHER_LAB_PM * PM_LAB + B_QR_OTHER_LAB_PT * PT_LAB
            + B_QR_OTHER_NO_LAB * NO_LAB
            + B_QR_OTHER_T_VEHICULO * T_VEHICULO + B_QR_OTHER_N_TRASBORDOS * N_TRASBORDOS
            + B_QR_OTHER_T_ESPERA_TRASB * T_ESPERA_TRASB + B_QR_OTHER_T_ESPERA_INI * T_ESPERA_INI
        )
    else:
        raise ValueError(f"spec inválida: {spec}")

    V = {CHOICE_BIP: V_BIP, CHOICE_QR_RED: V_QR_RED, CHOICE_QR_OTHER: V_QR_OTHER}
    av = {CHOICE_BIP: 1, CHOICE_QR_RED: 1, CHOICE_QR_OTHER: 1}

    logprob = models.lognested(V, av, nests_structure, CHOICE_NESTED)
    biodb = db.Database(f"db_nl_ext_{spec}_{partition}", pandas_df)
    b = bio.BIOGEME(biodb, logprob)
    b.model_name = model_name

    _estimate_and_write(b=b, out_dir=out_dir, model_name=model_name)
    print(f"✅ OK extendido {spec} | {partition} | {sample_label}")

    try:
        del pandas_df, biodb, b, logprob, V, av, nests_structure
    except Exception:
        pass
    gc.collect()
    time.sleep(1)

print("✅ Estimation helpers listos.")
```

---

## 3) Estimación — Nested Logit Base

> Estas celdas están en `eval: false` para evitar ejecuciones accidentales.

### 3.0) Driver (secuencial: múltiples muestras y/o particiones)

```{python}
#| label: run-nl-base-sequential
#| eval: false

print("label: run-nl-base-sequential")

# Ejecuta base en el orden definido (BASE_RUN_ORDER) para todas las particiones y samples.
for partition in RUN_PARTITIONS:
    for sample_label, frac in SAMPLE_CONFIGS.items():
        for spec in BASE_RUN_ORDER:
            print("\n" + "-" * 80)
            frac_txt = "full" if frac is None else f"{int(frac*100)}%"
            print(f"--- BASE | {partition} | {spec} | {sample_label} ({frac_txt}) ---")
            print("-" * 80)
            estimate_nested_logit_base(
                partition=partition,
                spec=spec,
                sample_label=sample_label,
                sample_fraction=frac,
                seed=SEED,
            )
```

### 3.1) Base (generic)

```{python}
#| label: estimate-nl-base-generic
#| eval: false

print("label: estimate-nl-base-generic")

for partition in RUN_PARTITIONS:
    estimate_nested_logit_base(
        partition=partition,
        spec="generic",
        sample_label="sample20pct",
        sample_fraction=SAMPLE_CONFIGS["sample20pct"],
        seed=SEED,
    )
```

### 3.2) Base (specific)

```{python}
#| label: estimate-nl-base-specific
#| eval: false

print("label: estimate-nl-base-specific")

for partition in RUN_PARTITIONS:
    estimate_nested_logit_base(
        partition=partition,
        spec="specific",
        sample_label="sample20pct",
        sample_fraction=SAMPLE_CONFIGS["sample20pct"],
        seed=SEED,
    )
```

---

## 4) Estimación — Nested Logit Extendido (sin caminata)

### 4.0) Driver (extendido: specific → generic, todas las muestras)

```{python}
#| label: run-nl-extended-specific-then-generic
#| eval: false

print("label: run-nl-extended-specific-then-generic")

for partition in RUN_PARTITIONS:
    for sample_label, frac in SAMPLE_CONFIGS.items():
        # Orden pedido: primero specific, luego generic
        for spec in EXTENDED_RUN_ORDER:
            print("\n" + "-" * 80)
            frac_txt = "full" if frac is None else f"{int(frac*100)}%"
            print(f"--- EXTENDIDO | {partition} | {spec} | {sample_label} ({frac_txt}) ---")
            print("-" * 80)
            estimate_nested_logit_extended(
                partition=partition,
                spec=spec,
                sample_label=sample_label,
                sample_fraction=frac,
                seed=SEED,
            )
```

### 4.0b) Driver (extendido: specific con 20% y luego 50%)

```{python}
#| label: run-nl-extended-specific-sample20-50
#| eval: false

print("label: run-nl-extended-specific-sample20-50")

for partition in RUN_PARTITIONS:
    for sample_label in ["sample20pct", "sample50pct"]:
        frac = SAMPLE_CONFIGS[sample_label]
        print("\n" + "-" * 80)
        print(f"--- EXTENDIDO | {partition} | specific | {sample_label} ({int(frac*100)}%) ---")
        print("-" * 80)
        estimate_nested_logit_extended(
            partition=partition,
            spec="specific",
            sample_label=sample_label,
            sample_fraction=SAMPLE_CONFIGS[sample_label],
            seed=SEED,
        )
```

### 4.1) Extendido (generic)

```{python}
#| label: estimate-nl-extended-generic
#| eval: false

print("label: estimate-nl-extended-generic")

for partition in RUN_PARTITIONS:
    estimate_nested_logit_extended(
        partition=partition,
        spec="generic",
        sample_label="sample20pct",
        sample_fraction=SAMPLE_CONFIGS["sample20pct"],
        seed=SEED,
    )
```

### 4.2) Extendido (specific)

```{python}
#| label: estimate-nl-extended-specific
#| eval: false

print("label: estimate-nl-extended-specific")

for partition in RUN_PARTITIONS:
    estimate_nested_logit_extended(
        partition=partition,
        spec="specific",
        sample_label="sample30pct",
        sample_fraction=SAMPLE_CONFIGS["sample30pct"],
        seed=SEED,
    )
```

---

## 5) Resultados (cargar CSVs)

```{python}
#| label: load-results

print("label: load-results")

from typing import Optional

SHOW_PARTITION = "2025-W17"
SHOW_SAMPLE = "sample20pct"

def _load_one_run(folder: Path, model_name: str):
    params_path = folder / f"params_{model_name}.csv"
    stats_path = folder / f"stats_{model_name}.csv"
    if not params_path.exists() or not stats_path.exists():
        return None, None
    return pd.read_csv(params_path), pd.read_csv(stats_path)

def _sort_specific_params(df_params: pd.DataFrame) -> pd.DataFrame:
    df = df_params.copy()
    df["Param"] = df["Name"].astype(str).str.replace(f"_{SHOW_PARTITION}$", "", regex=True)

    def key(p: str):
        if p == "MU_QR":
            return (0, 0, 0, p)
        if p.startswith("ASC_"):
            alt_rank = 0 if "RED" in p else 1 if "OTHER" in p else 2
            return (1, alt_rank, 0, p)
        if p.startswith("B_QR_"):
            parts = p.split("_")
            alt = parts[2] if len(parts) >= 3 else ""
            base = "_".join(parts[3:]) if len(parts) > 3 else ""
            alt_rank = 0 if alt == "RED" else 1 if alt == "OTHER" else 2
            return (2, 0, alt_rank, base)
        return (9, 9, 9, p)

    df["_k"] = df["Param"].astype(str).map(key)
    df = df.sort_values("_k").drop(columns=["_k"])
    return df

print("✅ Loader listo.")
```

### 5.1) Base — generic

```{python}
#| label: show-base-generic

print("label: show-base-generic")

year, week = SHOW_PARTITION.split("-W")
folder = BASE_OUTPUT_DIR / f"W{week}-nested-base-generic-{SHOW_SAMPLE}"
model_name = f"nested_logit_base_generic_{SHOW_PARTITION}"

params, stats = _load_one_run(folder, model_name)
print("📁", folder)
if params is None:
    print("⚠️ No hay outputs (corre primero la celda de estimación).")
else:
    display(stats)
    params["Param"] = params["Name"].astype(str).str.replace(f"_{SHOW_PARTITION}$", "", regex=True)
    cols = [c for c in ["Param", "Value", "Robust std err.", "Robust t-stat.", "Robust p-value"] if c in params.columns]
    display(params[cols].sort_values("Param"))
```

### 5.2) Base — specific

```{python}
#| label: show-base-specific

print("label: show-base-specific")

year, week = SHOW_PARTITION.split("-W")
folder = BASE_OUTPUT_DIR / f"W{week}-nested-base-specific-{SHOW_SAMPLE}"
model_name = f"nested_logit_base_specific_{SHOW_PARTITION}"

params, stats = _load_one_run(folder, model_name)
print("📁", folder)
if params is None:
    print("⚠️ No hay outputs (corre primero la celda de estimación).")
else:
    display(stats)
    cols = [c for c in ["Name", "Value", "Robust std err.", "Robust t-stat.", "Robust p-value"] if c in params.columns]
    params = params[cols]
    params = _sort_specific_params(params.rename(columns={"Name": "Name"}))
    display(params.rename(columns={"Name": "Name"}))
```

### 5.3) Extendido — generic

```{python}
#| label: show-extended-generic

print("label: show-extended-generic")

year, week = SHOW_PARTITION.split("-W")
folder = EXT_OUTPUT_DIR / f"W{week}-nested-ext-generic-{SHOW_SAMPLE}"
model_name = f"nested_logit_ext_generic_{SHOW_PARTITION}"

params, stats = _load_one_run(folder, model_name)
print("📁", folder)
if params is None:
    print("⚠️ No hay outputs (corre primero la celda de estimación).")
else:
    display(stats)
    params["Param"] = params["Name"].astype(str).str.replace(f"_{SHOW_PARTITION}$", "", regex=True)
    cols = [c for c in ["Param", "Value", "Robust std err.", "Robust t-stat.", "Robust p-value"] if c in params.columns]
    display(params[cols].sort_values("Param"))
```

### 5.4) Extendido — specific

```{python}
#| label: show-extended-specific

print("label: show-extended-specific")

year, week = SHOW_PARTITION.split("-W")
folder = EXT_OUTPUT_DIR / f"W{week}-nested-ext-specific-{SHOW_SAMPLE}"
model_name = f"nested_logit_ext_specific_{SHOW_PARTITION}"

params, stats = _load_one_run(folder, model_name)
print("📁", folder)
if params is None:
    print("⚠️ No hay outputs (corre primero la celda de estimación).")
else:
    display(stats)
    cols = [c for c in ["Name", "Value", "Robust std err.", "Robust t-stat.", "Robust p-value"] if c in params.columns]
    df = params[cols].copy()
    df = _sort_specific_params(df.rename(columns={"Name": "Name"}))
    # Normalizar columna "Name" -> "Param" para lectura rápida
    df["Param"] = df["Name"].astype(str).str.replace(f"_{SHOW_PARTITION}$", "", regex=True)
    df = df.drop(columns=["Name"])
    display(df)
```



## 6) Análisis e Interpretación — NL Base

### 6.1) Diagnóstico de convergencia (generic vs specific)

```{python}
#| label: analyze-nl-base-convergence

print("label: analyze-nl-base-convergence")

import numpy as np
import pandas as pd

ANALYZE_PARTITION = SHOW_PARTITION
ANALYZE_SAMPLES = ["sample20pct", "sample40pct"]
ANALYZE_SPECS = ["generic", "specific"]

year, week = ANALYZE_PARTITION.split("-W")

rows = []
for spec in ANALYZE_SPECS:
    for sample in ANALYZE_SAMPLES:
        folder = BASE_OUTPUT_DIR / f"W{week}-nested-base-{spec}-{sample}"
        model_name = f"nested_logit_base_{spec}_{ANALYZE_PARTITION}"
        params, stats = _load_one_run(folder, model_name)

        if stats is None or params is None:
            rows.append({
                "partition": ANALYZE_PARTITION,
                "spec": spec,
                "sample": sample,
                "status": "missing",
            })
            continue

        grad_norm = pd.to_numeric(stats["Final gradient norm"], errors="coerce").iloc[0]
        ll_final = pd.to_numeric(stats["Final log likelihood"], errors="coerce").iloc[0]
        rho_init = pd.to_numeric(stats["Rho-square for the init. model"], errors="coerce").iloc[0]
        rho_null = pd.to_numeric(stats["Rho-square (vs null equal shares)"], errors="coerce").iloc[0]

        params = params.copy()
        params["Param"] = params["Name"].astype(str).str.replace(f"_{ANALYZE_PARTITION}$", "", regex=True)
        mu_val = pd.to_numeric(
            params.loc[params["Param"] == "MU_QR", "Value"],
            errors="coerce",
        )
        mu_qr = float(mu_val.iloc[0]) if len(mu_val) else np.nan

        # Heurística práctica de calidad numérica para comparar corridas.
        # No reemplaza el mensaje oficial de convergencia de Biogeme.
        likely_issue = bool((pd.notna(grad_norm) and grad_norm > 20) or (pd.notna(mu_qr) and mu_qr > 20))
        quality = "warning" if likely_issue else "ok"

        rows.append({
            "partition": ANALYZE_PARTITION,
            "spec": spec,
            "sample": sample,
            "ll_final": ll_final,
            "rho_vs_init": rho_init,
            "rho_vs_null": rho_null,
            "grad_norm": grad_norm,
            "mu_qr": mu_qr,
            "quality_flag": quality,
        })

df_diag = pd.DataFrame(rows)
display(df_diag)
```

### 6.2) Comparación de parámetros — Base specific (20% vs 40%)

```{python}
#| label: compare-nl-base-specific

print("label: compare-nl-base-specific")

partition = SHOW_PARTITION
year, week = partition.split("-W")

def _load_base_specific(sample_label: str):
    folder = BASE_OUTPUT_DIR / f"W{week}-nested-base-specific-{sample_label}"
    model_name = f"nested_logit_base_specific_{partition}"
    p, s = _load_one_run(folder, model_name)
    if p is None or s is None:
        return None, None
    p = p.copy()
    p["Param"] = p["Name"].astype(str).str.replace(f"_{partition}$", "", regex=True)
    return p, s

p20, s20 = _load_base_specific("sample20pct")
p40, s40 = _load_base_specific("sample40pct")

if p20 is None or p40 is None:
    print("⚠️ Faltan corridas specific para comparar 20% vs 40%.")
else:
    c20 = p20[["Param", "Value", "Robust t-stat."]].rename(
        columns={"Value": "beta_20", "Robust t-stat.": "t_20"}
    )
    c40 = p40[["Param", "Value", "Robust t-stat."]].rename(
        columns={"Value": "beta_40", "Robust t-stat.": "t_40"}
    )
    cmp = c20.merge(c40, on="Param", how="outer")
    cmp = cmp.sort_values("Param")
    display(cmp)
```

### 6.3) Interpretación (NL base)

Con base en las corridas observadas, la lectura sustantiva de `NL base` es:

1.  **Modelo a reportar**: usar `specific` como referencia principal. `generic` se puede dejar como control, pero no para inferencia cuando no converge o entrega `MU_QR` extremo.

2.  **Probabilidad base de QR vs BIP**: tanto `ASC_QR_RED` como `ASC_QR_OTHER` son negativos, lo que implica que en la condición base (`LAB_VALLE`) el uso de BIP sigue siendo más probable que QR. Además, `ASC_QR_OTHER` es menos negativo que `ASC_QR_RED`, sugiriendo mayor afinidad relativa por `QR_OTHER` que por `QR_RED` en la base.

3.  **Efecto horario/laboral para QR_RED**: los coeficientes `B_QR_RED_LAB_PM`, `B_QR_RED_LAB_PT` y `B_QR_RED_NO_LAB` son positivos. En palabras: el uso de **QR_RED es más probable** en punta mañana laboral, punta tarde laboral y no laboral respecto de `LAB_VALLE`, con mayor intensidad en `LAB_PT`.

4.  **Efecto horario/laboral para QR_OTHER**: los coeficientes `B_QR_OTHER_LAB_PM`, `B_QR_OTHER_LAB_PT` y `B_QR_OTHER_NO_LAB` también son positivos. En palabras: el uso de **QR_OTHER es más probable** en esos tres contextos respecto de `LAB_VALLE`, y también suele destacar `LAB_PT`.

5.  **Comparación entre tipos de QR**: típicamente, en horas punta laborales (`LAB_PM`/`LAB_PT`) el incremento de utilidad es mayor para `QR_RED` que para `QR_OTHER`, mientras que en `NO_LAB` la diferencia entre alternativas puede estrecharse o invertirse según muestra.

6.  **Estructura del nido QR**: en `specific`, `MU_QR` se mantiene en un rango estable y plausible entre muestras (20%/40%), consistente con correlación intra-nido en alternativas QR sin colapso numérico.

En síntesis: para `NL base`, la evidencia apunta a que QR se vuelve relativamente más atractivo fuera de la base `LAB_VALLE`, especialmente en punta tarde laboral, y que la especificación `specific` entrega resultados más robustos y útiles para interpretación.

## 7) Análisis e Interpretación — NL Extendido (specific)

La lectura sustantiva del `NL extendido specific` (corrida de referencia en `5.4`) es:

1.  **Ajuste global fuerte**: el modelo mantiene un buen desempeño agregado (`Rho-square` alrededor de `0.527`), consistente con lo observado en corridas previas de extendido.

2.  **Preferencia base (vs BIP)**: `ASC_QR_RED` y `ASC_QR_OTHER` son negativos. En la condición base temporal (`LAB_VALLE`), la alternativa de referencia (`BIP`) sigue siendo más probable. Además, `ASC_QR_OTHER` suele ser menos negativo que `ASC_QR_RED`, indicando mayor propensión relativa a `QR_OTHER` en la base.

3.  **Efectos temporales (dummies V2)**:
    - Para `QR_RED`, `B_QR_RED_LAB_PM`, `B_QR_RED_LAB_PT` y `B_QR_RED_NO_LAB` son positivos y significativos.
    - Para `QR_OTHER`, `B_QR_OTHER_LAB_PM`, `B_QR_OTHER_LAB_PT` y `B_QR_OTHER_NO_LAB` también son positivos y significativos.
    
    En términos de comportamiento: la probabilidad relativa de usar QR sube en `LAB_PM`, `LAB_PT` y `NO_LAB` respecto de `LAB_VALLE`; `LAB_PT` es el aumento más intenso para ambas alternativas QR.

4.  **Atributos de viaje**:
    - `N_TRASBORDOS` y tiempos de espera (`T_ESPERA_INI`, `T_ESPERA_TRASB`) aparecen con signo negativo y significativos en ambas alternativas QR: más fricción operacional reduce utilidad de QR.
    - `T_VEHICULO` muestra heterogeneidad: en `QR_RED` no necesariamente es significativo en esta corrida, mientras que en `QR_OTHER` puede aparecer significativo con signo pequeño.

5.  **Parámetro del nido (`MU_QR`)**:
    - Un `MU_QR` no significativo (p-valor alto y error estándar grande) **no invalida automáticamente todo el modelo**.
    - Nota técnica: el `p-value` estándar reportado por Biogeme se calcula respecto de `0`; para nested, la comparación más útil es contrastar cercanía a `MU_QR = 1` (caso tipo MNL).
    - Sí indica que, en esta muestra/especificación, la evidencia de correlación intra-nido QR por sobre una estructura tipo MNL es débil.
    - Implicancia práctica: reportar con cautela la interpretación de la estructura de nido y priorizar la lectura de signos/magnitudes de utilidades observables.

En síntesis: el `NL extendido specific` es útil y consistente para interpretar patrones temporales y fricciones de viaje en la adopción QR; la principal cautela está en no sobrerreclamar evidencia sobre la fortaleza del nido cuando `MU_QR` no es estadísticamente robusto.
